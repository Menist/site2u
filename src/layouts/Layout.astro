---
import '@fontsource/open-sans/300.css';
import '@fontsource/open-sans/400.css';
import '@fontsource/open-sans/600.css';
import '@fontsource/open-sans/700.css';
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import '../styles/global.css'
const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "site2u.by",
  "url": "https://site2u.by",
  "telephone": "+375296195265",
  "email": "imc@imc.by",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "пр. Независимости, д. 68-2, к.108",
    "addressLocality": "Минск",
    "addressCountry": "BY"
  }
};

interface Props {
  title: string;
  description: string;
  serviceSchema?: object;
  transparent?: boolean
  bodyClass?: string
}

const {title, description, serviceSchema, transparent = false} = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL('/og-image.jpg', Astro.site);
const { bodyClass = '' } = Astro.props;
---
<!doctype html>
<html lang="ru">
<head>
  <!-- 1. Кодировка — всегда первой -->
  <meta charset="UTF-8"/>

  <!-- 2. Viewport -->
  <meta name="viewport" content="width=device-width, initial-scale=1"/>

  <!-- 3. Критичные инлайн-стили (js-loading) -->
  <style is:global>
    .js-loading .hero__subtitle,
    .js-loading .hero__cta,
    .js-loading .hero__arrow {
      opacity: 0 !important;
    }
    .js-loading .services,
    .js-loading .about-teaser,
    .js-loading .advantages,
    .js-loading .process,
    .js-loading .configurator,
    .js-loading .cta {
      opacity: 0 !important;
      transform: translateY(35px) !important;
    }
  </style>

  <!-- 4. Скрипт js-loading (инлайн, блокирующий — это правильно) -->
  <script is:inline>
    document.documentElement.classList.add('js-loading');
  </script>

  <!-- 5. Title и мета -->
  <title>{title}</title>
  <meta name="description" content={description}/>
  <meta name="generator" content={Astro.generator}/>
  <meta name="google-site-verification" content="xTSWs3ffQhgcm-tiYO9HUXO1Qh3iPW0TaNlNgdw703U" />
  <meta name="yandex-verification" content="ad8369549c1620cd" />

  <!-- 6. Canonical и OG -->
  <link rel="canonical" href={canonicalURL}/>
  <meta property="og:title" content={title}/>
  <meta property="og:description" content={description}/>
  <meta property="og:url" content={canonicalURL}/>
  <meta property="og:image" content={ogImageURL}/>
  <meta property="og:type" content="website"/>

  <!-- 7. Фавиконки -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg"/>
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png"/>
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png"/>
  <link rel="icon" href="/favicon.ico"/>
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png"/>
  <link rel="manifest" href="/site.webmanifest"/>

  <!-- 8. Schema.org -->
  <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)}/>
  {serviceSchema && (
    <script type="application/ld+json" set:html={JSON.stringify(serviceSchema)}/>
  )}

  <!-- 9. CSS -->
  <link rel="stylesheet" href="/animations.css"/>

  <!-- 10. Скрипты defer -->
  <script defer src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  <script defer src="/animations.js"></script>
  <script defer src="/validation.js"></script>

  <!-- 11. Аналитика (последней, через idle) -->
  <script is:inline>
    function loadAnalytics() {
      // Google Analytics
      const gaScript = document.createElement('script');
      gaScript.async = true;
      gaScript.src = 'https://www.googletagmanager.com/gtag/js?id=G-9EL42XVXEB';
      document.head.appendChild(gaScript);
      gaScript.onload = function () {
        window.dataLayer = window.dataLayer || [];

        function gtag() {
          dataLayer.push(arguments);
        }

        window.gtag = gtag;
        gtag('js', new Date());
        gtag('config', 'G-9EL42XVXEB');
      };

      // Яндекс.Метрика
      (function (m, e, t, r, i, k, a) {
        m[i] = m[i] || function () {
          (m[i].a = m[i].a || []).push(arguments)
        };
        m[i].l = 1 * new Date();
        k = e.createElement(t), a = e.getElementsByTagName(t)[0], k.async = 1, k.src = r, a.parentNode.insertBefore(k, a)
      })(window, document, 'script', 'https://mc.yandex.ru/metrika/tag.js?id=110113534', 'ym');
      ym(110113534, 'init', {
        ssr: true,
        webvisor: true,
        clickmap: true,
        accurateTrackBounce: true,
        trackLinks: true
      });
    }

    if ('requestIdleCallback' in window) {
      requestIdleCallback(loadAnalytics);
    } else {
      setTimeout(loadAnalytics, 2000);
    }
  </script>
  <noscript>
    <div><img src="https://mc.yandex.ru/watch/110113534" style="position:absolute;left:-9999px;" alt=""/></div>
  </noscript>

  <!-- 12. Трекинг кликов -->
  <script is:inline>
    document.addEventListener('click', function (e) {
      const target = e.target.closest('[data-track]');
      if (!target) return;

      const eventName = target.dataset.track;

      if (typeof gtag !== 'undefined') {
        gtag('event', eventName, {
          event_category: 'click',
          event_label: target.textContent.trim()
        });
      }

      if (typeof ym !== 'undefined') {
        ym(110113534, 'reachGoal', eventName);
      }
    });
  </script>
</head>
<body class={bodyClass}>
<Header transparent={transparent}/>
<main>
  <slot/>
</main>
<Footer/>


</body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  main {
    flex: 1;
  }


  @media (max-width: 768px) {
    html,
    body {
      min-height: 100vh;
      min-height: 100dvh;
    }
  }
</style>